{
  "name": "typst-sidecar-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "scripts/build-worker.sh",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.92.0"
  }
}

