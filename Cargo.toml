[package]
name = "sidecar-host"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmi = { version = "1.0.9", default-features = false, features = ["std"] }

[profile.release]
panic = "abort"
opt-level = "s"
debug = 0
debug-assertions = false
incremental = false
